import {createRoutinesHandler} from "../../../src/http/routes/createRoutinesHandler";

test("Should get routine from ctx", () => {
    // const ctx: any = {};
    // const mockNextFunction = jest.fn().mockImplementation();
    //
    // let routines = createRoutinesHandler();
    // routines(ctx, mockNextFunction);
    //
    // expect(mockNextFunction).toHaveBeenCalled()

});
